"use client";
import { Minus, Plus, X } from "lucide-react"; import Link from "next/link"; import { useCart } from "@/context/CartContext";
export default function CartDrawer({ onClose }: { onClose: () => void }) { const { items, cartTotal, updateQuantity, removeFromCart } = useCart(); return <div className="fixed inset-0 z-50 bg-charcoal/30" onClick={onClose}><aside onClick={e => e.stopPropagation()} className="ml-auto flex h-full w-full max-w-md flex-col bg-white p-5 shadow-2xl"><div className="flex items-center justify-between"><h2 className="text-xl font-bold">Your bag <span className="text-sm font-normal text-charcoal/50">({items.length})</span></h2><button onClick={onClose} className="grid h-11 w-11 place-items-center"><X/></button></div>{items.length === 0 ? <div className="flex flex-1 flex-col items-center justify-center text-center"><p className="font-semibold">Your bag is waiting.</p><p className="mt-2 text-sm text-charcoal/55">Add something useful for your next campus day.</p></div> : <><div className="flex-1 space-y-4 overflow-y-auto py-6">{items.map(i => <div className="flex gap-3" key={i.id}><img src={i.image} alt="" className="h-16 w-16 rounded-xl object-cover"/><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{i.name}</p><p className="mt-1 text-sm">৳{i.price.toLocaleString()}</p><div className="mt-2 flex items-center gap-3"><button onClick={() => updateQuantity(i.id, i.quantity - 1)}><Minus size={14}/></button><span className="text-sm">{i.quantity}</span><button onClick={() => updateQuantity(i.id, i.quantity + 1)}><Plus size={14}/></button><button className="ml-auto text-xs text-coral" onClick={() => removeFromCart(i.id)}>Remove</button></div></div></div>)}</div><div className="border-t border-charcoal/10 pt-5"><div className="mb-4 flex justify-between font-bold"><span>Subtotal</span><span>৳{cartTotal.toLocaleString()}</span></div><Link onClick={onClose} href="/checkout" className="block rounded-full bg-charcoal py-4 text-center font-semibold text-white">Go to checkout</Link></div></>}</aside></div>; }
