"use client";
import { Heart, Plus } from "lucide-react";
import { Product } from "@/data/products";
import { useCart } from "@/context/CartContext";
export default function ProductCard({ product }: { product: Product }) { const { addToCart, wishlist, toggleWishlist } = useCart(); const saved = wishlist.includes(product.id); return <article className="group rounded-2xl bg-card p-3 shadow-soft transition hover:-translate-y-1"><div className="relative aspect-square overflow-hidden rounded-xl bg-[#eeeae1]"><img src={product.image} alt={product.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-105" /><button aria-label="Save item" onClick={() => toggleWishlist(product.id)} className="absolute right-3 top-3 grid h-10 w-10 place-items-center rounded-full bg-white/85"> <Heart size={17} fill={saved ? "#f96a66" : "none"} color={saved ? "#f96a66" : "#414242"} /></button></div><div className="px-1 pb-1 pt-4"><div className="mb-2 flex items-center justify-between gap-2"><span className="text-xs font-semibold uppercase tracking-wider text-charcoal/55">{product.category}</span><span className={`text-xs font-semibold ${product.stock ? "text-emerald-600" : "text-coral"}`}>{product.stock ? "In stock" : "Out of stock"}</span></div><h3 className="min-h-[48px] font-semibold leading-6">{product.name}</h3><p className="mt-1 text-sm text-charcoal/55">{product.specs}</p><div className="mt-4 flex items-center justify-between"><span className="text-lg font-bold">৳{product.price.toLocaleString()}</span><button disabled={!product.stock} onClick={() => addToCart(product)} className="grid h-11 w-11 place-items-center rounded-full bg-charcoal text-white disabled:bg-charcoal/15 disabled:text-charcoal/30"><Plus size={19} /></button></div></div></article>; }
