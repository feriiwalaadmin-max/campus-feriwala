"use client";
import { useMemo, useState } from "react"; import ProductCard from "@/components/ProductCard"; import { Category } from "@/data/products"; import { useCart } from "@/context/CartContext";
const cats = ["All", "Neckband", "Earbuds", "Powerbank", "Desk Lamp", "Mouse & Pad", "Keyboard Cleaner"];
export default function Products() { const { allProducts } = useCart(); const [cat, setCat] = useState("All"); const [max, setMax] = useState(1500); const visible = useMemo(() => allProducts.filter(p => (cat === "All" || p.category === cat) && p.price <= max), [allProducts, cat, max]); return <main className="container py-14"><div className="max-w-xl"><p className="text-sm font-semibold text-orange">The catalog</p><h1 className="mt-3 text-4xl font-black tracking-tight sm:text-6xl">Find your next<br/>campus essential.</h1></div><div className="no-scrollbar mt-12 flex gap-2 overflow-x-auto pb-2">{cats.map(c => <button key={c} onClick={() => setCat(c)} className={`min-h-11 shrink-0 rounded-full px-4 text-sm font-semibold ${cat === c ? "bg-charcoal text-white" : "bg-white text-charcoal/65"}`}>{c}</button>)}</div><div className="mt-5 flex max-w-sm items-center gap-4 rounded-xl bg-white px-4 py-3"><label htmlFor="price" className="whitespace-nowrap text-sm font-semibold">Up to ৳{max}</label><input id="price" type="range" min="250" max="1500" step="50" value={max} onChange={e => setMax(Number(e.target.value))} className="w-full accent-orange"/></div><p className="mt-10 text-sm text-charcoal/55">{visible.length} products</p><div className="mt-4 grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-3">{visible.map(p => <ProductCard key={p.id} product={p}/>)}</div>{visible.length === 0 && <p className="py-20 text-center text-charcoal/55">No gadgets fit those filters.</p>}</main>; }
